{
  "name": "next-js-module-federation",
  "version": "1.0.0",
  "main": "index.js",
  "private": "true",
  "repository": "https://github.com/efoxTeam/next-js-module-federation",
  "author": "xuhongbin <xuhongbin@yy.com>",
  "license": "MIT",
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "projects/*"
  ],
  "devDependencies": {
    "@efox/eslint-config-react-prittier-ts": "^1.2.3"
  },
  "scripts": {
    "dev": "lerna run --scope={app1,static-host} --parallel dev",
    "build": "lerna run --scope={app1,static-host} --parallel build",
    "start": "lerna run --scope={app1,static-host} --parallel start",
    "dev:all": "lerna run --scope={app1,next-host,static-host} --parallel dev",
    "build:all": "lerna run --scope={app1,next-host,static-host} --parallel build",
    "start:all": "lerna run --scope={app1,next-host,static-host} --parallel start"
  }
}
