{
  "name": "next-js-module-federation",
  "version": "1.0.0",
  "main": "index.js",
  "private": "true",
  "repository": "https://github.com/efoxTeam/next-js-module-federation",
  "author": "xuhongbin",
  "license": "MIT",
  "workspaces": [
    "projects/*"
  ],
  "devDependencies": {
    "@efox/eslint-config-react-prittier-ts": "^1.2.3"
  },
  "scripts": {
    "dev": "pnpm run --filter={app1,static-host} --parallel dev",
    "build": "pnpm run --filter={app1,static-host} --parallel build",
    "start": "pnpm run --filter={app1,static-host} --parallel start",
    "dev:all": "pnpm run --filter={app1,next-host,static-host} --parallel dev",
    "build:all": "pnpm run --filter={app1,next-host,static-host} --parallel build",
    "start:all": "pnpm run --filter={app1,next-host,static-host} --parallel start",
    "preinstall": "npx only-allow pnpm"
  }
}
